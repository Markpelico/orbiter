# The platform stack: everything ORBITER runs on, built to be DISPOSABLE.
# `make up` creates it, `make down` deletes it, and the delta between those
# two commands is the entire cloud bill (~$0.30/hour while up).
#
# State lives in the bootstrap-created bucket with S3-lockfile locking.
terraform {
  required_version = ">= 1.10"

  backend "s3" {
    bucket       = "orbiter-tfstate-907797501994"
    key          = "platform/terraform.tfstate"
    region       = "us-east-1"
    use_lockfile = true
  }

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
    helm = {
      source  = "hashicorp/helm"
      version = "~> 3.0"
    }
    kubernetes = {
      source  = "hashicorp/kubernetes"
      version = "~> 2.30"
    }
    random = {
      source  = "hashicorp/random"
      version = "~> 3.6"
    }
  }
}

provider "aws" {
  region = var.region
  default_tags {
    tags = {
      project    = "orbiter"
      managed_by = "terraform"
      stack      = "platform"
    }
  }
}

# Cluster credentials via short-lived exec tokens — nothing stored.
provider "kubernetes" {
  host                   = module.eks.cluster_endpoint
  cluster_ca_certificate = base64decode(module.eks.cluster_certificate_authority_data)
  exec {
    api_version = "client.authentication.k8s.io/v1beta1"
    command     = "aws"
    args        = ["eks", "get-token", "--cluster-name", module.eks.cluster_name]
  }
}

provider "helm" {
  kubernetes = {
    host                   = module.eks.cluster_endpoint
    cluster_ca_certificate = base64decode(module.eks.cluster_certificate_authority_data)
    exec = {
      api_version = "client.authentication.k8s.io/v1beta1"
      command     = "aws"
      args        = ["eks", "get-token", "--cluster-name", module.eks.cluster_name]
    }
  }
}
